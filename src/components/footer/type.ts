export type bottomNavItemType = {
	text: string;
	icon: any;
	href: string;
};

export type DetailFooterProps = {
	facilityId: number;
	disable: boolean;
};

export type ReverseFooterProps = {
	disable: boolean;
};
